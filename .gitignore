.DS_Store
*.swp
*.swo
.cursor-managed-skills-manifest.json

# Knowledge engine
content/raw/
content/manifest.json
.venv-nke/

# Vendored third-party skill packs (cloned by scripts/vendor-install.sh to
# the pinned SHAs in vendor-pins.txt). Commit the pins, not the trees.
.vendor/
